# URL base del backend del formulario RAG (server/). Vacío = usa "/api" relativo.
VITE_API_URL=

# URL base de la API de artículos del blog (proyecto separado "seo-article-forge").
# Vacío = la página /blog/ muestra el estado vacío, sin intentar pedir nada.
VITE_BLOG_API_URL=
